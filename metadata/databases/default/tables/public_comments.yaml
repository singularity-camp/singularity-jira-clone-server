table:
  schema: public
  name: comments
object_relationships:
  - name: card
    using:
      foreign_key_constraint_on: card_id
insert_permissions:
  - role: guest
    permission:
      check: {}
      columns:
        - content
        - card_id
        - id
      backend_only: false
select_permissions:
  - role: guest
    permission:
      columns:
        - content
        - card_id
        - id
      filter: {}
      allow_aggregations: true
update_permissions:
  - role: guest
    permission:
      columns:
        - content
        - card_id
        - id
      filter: {}
      check: {}
delete_permissions:
  - role: guest
    permission:
      filter: {}
